[project]
name = "rag-platform"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi",
  "uvicorn[standard]",
  "python-dotenv",
  "pydantic",
]

[tool.ruff]
line-length = 100
select = ["E", "F", "I", "B", "UP"]
ignore = []

[tool.black]
line-length = 100
